---
phase: ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/src/styles/main.css
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "CSS uses only declared variable tokens (no hardcoded colors)"
    - "Typography follows 4-role scale (12px, 14px, 18px, 24px)"
    - "Spacing uses declared tokens (xs, sm, md, lg, xl, 2xl)"
    - "Duplicate CSS file deleted"
    - "All component classes defined in single index.css"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Complete design system with variables and component classes"
      min_lines: 800
      contains: ":root"
    - path: "frontend/src/styles/main.css"
      provides: "Deleted (was duplicate dead code)"
  key_links:
    - from: "frontend/src/index.css"
      to: "CSS variables"
      via: ":root declarations"
      pattern: "var\\(--"
---

<objective>
Establish the CSS foundation and design system for the UI redesign.

Purpose: Create a single source of truth for all visual styling that downstream plans can reference. Eliminate the duplicate CSS file and hardcoded colors identified in the UI review.

Output: Clean `index.css` with all design tokens, component classes, and responsive breakpoints defined.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/ui-redesign/CONTEXT.md
@.planning/UI-SPEC.md
@.planning/ui-reviews/UI-REVIEW.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/index.css:
```css
:root {
  --bg-primary: #121212;
  --bg-secondary: #1e1e1e;
  --bg-tertiary: #2a2a2a;
  --fg-primary: #e0e0e0;
  --fg-secondary: #bbbbbb;
  --accent: #6200ea;
  --success: #4caf50;
  --danger: #dc3545;
  --warning: #f5a623;
  --border: #333333;
  --sidebar-width: 260px;
  --header-height: 60px;
  --radius: 8px;
  --transition: 0.2s ease;
}
```

From UI-SPEC.md spacing tokens:
```css
/* Add to :root */
--space-xs: 4px;
--space-sm: 8px;
--space-md: 16px;
--space-lg: 24px;
--space-xl: 32px;
--space-2xl: 48px;
```

From UI-SPEC.md typography:
```css
/* Body: 14px, weight 400, line-height 1.5 */
/* Label: 12px, weight 500, line-height 1.4 */
/* Heading: 18px, weight 600, line-height 1.3 */
/* Stat: 24px, weight 600, line-height 1.2 */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Delete duplicate CSS and fix hardcoded colors</name>
  <files>frontend/src/styles/main.css, frontend/src/index.css</files>
  
  <read_first>
    - frontend/src/index.css (current 1409-line file)
    - frontend/src/styles/main.css (duplicate to delete)
    - .planning/UI-SPEC.md (design tokens reference)
    - .planning/ui-reviews/UI-REVIEW.md (hardcoded color locations)
  </read_first>
  
  <action>
**Step 1: Delete duplicate CSS file**
Delete `frontend/src/styles/main.css` entirely. This file defines `--text-primary`, `--text-secondary`, `--text-muted` which conflict with `index.css`'s `--fg-primary`, `--fg-secondary`. Only `index.css` is imported.

**Step 2: Fix hardcoded colors in index.css**
Replace these specific hardcoded values with CSS variables:
- Line 76: `background-color: #333333;` → `background-color: var(--bg-tertiary);`
- Line 80: `color: #888;` → `color: var(--fg-secondary);`
- Line 119: `background-color: #333333;` → `background-color: var(--bg-tertiary);`
- Line 143: `color: #121212;` → `color: var(--bg-primary);`
- Search for any other hardcoded `#121212`, `#1e1e1e`, `#2a2a2a`, `#333333`, `#e0e0e0`, `#bbbbbb` and replace with appropriate variable references.

**Step 3: Add spacing tokens to :root**
After the existing variables, add:
```css
--space-xs: 4px;
--space-sm: 8px;
--space-md: 16px;
--space-lg: 24px;
--space-xl: 32px;
--space-2xl: 48px;
```

**Step 4: Add typography scale classes**
Add these utility classes:
```css
.text-body { font-size: 14px; font-weight: 400; line-height: 1.5; }
.text-label { font-size: 12px; font-weight: 500; line-height: 1.4; }
.text-heading { font-size: 18px; font-weight: 600; line-height: 1.3; }
.text-stat { font-size: 24px; font-weight: 600; line-height: 1.2; }
```
  </action>
  
  <verify>
    <automated>cd /root/Mcbot/minecraft-kit-bot && test ! -f frontend/src/styles/main.css && echo "PASS: main.css deleted" || echo "FAIL: main.css still exists"</automated>
  </verify>
  
  <acceptance_criteria>
    - `frontend/src/styles/main.css` does not exist
    - `frontend/src/index.css` contains `--space-xs: 4px`
    - `frontend/src/index.css` contains `.text-body`
    - `grep -c '#333333' frontend/src/index.css` returns 0 (all replaced with var(--border))
    - `grep -c '#888' frontend/src/index.css` returns 0 (all replaced with var(--fg-secondary))
  </acceptance_criteria>
  
  <done>Duplicate CSS deleted, hardcoded colors replaced with variables, spacing tokens and typography classes added to index.css</done>
</task>

<task type="auto">
  <name>task 2: Create component CSS classes</name>
  <files>frontend/src/index.css</files>
  
  <read_first>
    - frontend/src/index.css (after task 1 modifications)
    - .planning/UI-SPEC.md (component contracts section)
    - frontend/src/components/Layout/Layout.jsx (to understand current structure)
    - frontend/src/pages/Draftboard.jsx (sample page with inline styles)
  </read_first>
  
  <action>
Add comprehensive component classes to `index.css` following UI-SPEC.md contracts. Append these classes after the existing styles:

**Card classes:**
```css
.card {
  background: var(--bg-secondary);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: var(--space-md);
  transition: border-color var(--transition);
}
.card:hover {
  border-color: #444444;
}
.card-header {
  font-size: 18px;
  font-weight: 600;
  margin-bottom: var(--space-md);
}
```

**Button size variants (extend existing .btn):**
```css
.btn-sm { height: 32px; padding: 0 var(--space-md); font-size: 14px; }
.btn-lg { height: 40px; padding: 0 var(--space-lg); font-size: 14px; }
```

**Form classes:**
```css
.form-group { margin-bottom: var(--space-md); }
.form-label { font-size: 14px; font-weight: 500; color: var(--fg-secondary); margin-bottom: var(--space-xs); display: block; }
.form-input { height: 40px; background: var(--bg-tertiary); border: 1px solid var(--border); border-radius: 6px; padding: 0 var(--space-sm); }
.form-input:focus { border-color: var(--accent); }
.form-error { border-color: var(--danger); }
.form-error-text { color: var(--danger); font-size: 12px; margin-top: var(--space-xs); }
```

**Status indicator classes:**
```css
.status-dot { width: 8px; height: 8px; border-radius: 50%; display: inline-block; }
.status-online { background: var(--success); }
.status-warning { background: var(--warning); }
.status-offline { background: var(--danger); }
.status-inactive { background: #666666; }
.status-label { font-size: 12px; font-weight: 500; margin-left: var(--space-xs); }
```

**Table classes:**
```css
.table { width: 100%; border-collapse: collapse; }
.table th { font-size: 12px; font-weight: 500; color: var(--fg-secondary); text-align: left; padding: var(--space-sm); border-bottom: 1px solid var(--border); }
.table td { font-size: 14px; padding: var(--space-sm); border-bottom: 1px solid var(--border); }
.table tr:hover { background: var(--bg-tertiary); }
```

**Modal classes:**
```css
.modal-overlay { position: fixed; inset: 0; background: rgba(0, 0, 0, 0.7); display: flex; align-items: center; justify-content: center; z-index: 1000; }
.modal { background: var(--bg-secondary); border-radius: 12px; max-width: 480px; width: 90%; max-height: 90vh; overflow: auto; }
.modal-header { font-size: 18px; font-weight: 600; padding: var(--space-lg); border-bottom: 1px solid var(--border); }
.modal-body { padding: var(--space-lg); }
.modal-footer { padding: var(--space-lg); display: flex; justify-content: flex-end; gap: var(--space-sm); }
```

**Toast classes:**
```css
.toast-container { position: fixed; bottom: var(--space-lg); right: var(--space-lg); z-index: 2000; display: flex; flex-direction: column; gap: var(--space-sm); }
.toast { padding: var(--space-md); border-radius: var(--radius); min-width: 300px; max-width: 400px; }
.toast-success { background: var(--success); color: white; }
.toast-error { background: var(--danger); color: white; border-left: 4px solid var(--danger); }
.toast-warning { background: var(--warning); color: var(--bg-primary); }
```

**Layout classes:**
```css
.layout { display: flex; min-height: 100vh; }
.sidebar { width: var(--sidebar-width); background: var(--bg-secondary); border-right: 1px solid var(--border); position: fixed; top: 0; left: 0; bottom: 0; }
.main-content { margin-left: var(--sidebar-width); flex: 1; padding: var(--space-lg); }
.bottom-nav { display: none; position: fixed; bottom: 0; left: 0; right: 0; height: 56px; background: var(--bg-secondary); border-top: 1px solid var(--border); }
.nav-item { display: flex; flex-direction: column; align-items: center; justify-content: center; flex: 1; color: var(--fg-secondary); text-decoration: none; font-size: 12px; }
.nav-item.active { color: var(--accent); border-top: 2px solid var(--accent); }

@media (max-width: 768px) {
  .sidebar { display: none; }
  .main-content { margin-left: 0; padding-bottom: 56px; }
  .bottom-nav { display: flex; }
}
```

**Utility classes:**
```css
.flex { display: flex; }
.flex-col { flex-direction: column; }
.items-center { align-items: center; }
.justify-between { justify-content: space-between; }
.gap-xs { gap: var(--space-xs); }
.gap-sm { gap: var(--space-sm); }
.gap-md { gap: var(--space-md); }
.gap-lg { gap: var(--space-lg); }
.mt-sm { margin-top: var(--space-sm); }
.mt-md { margin-top: var(--space-md); }
.mt-lg { margin-top: var(--space-lg); }
.mb-sm { margin-bottom: var(--space-sm); }
.mb-md { margin-bottom: var(--space-md); }
.mb-lg { margin-bottom: var(--space-lg); }
.p-md { padding: var(--space-md); }
.p-lg { padding: var(--space-lg); }
```
  </action>
  
  <verify>
    <automated>cd /root/Mcbot/minecraft-kit-bot && grep -c "\.card {" frontend/src/index.css</automated>
  </verify>
  
  <acceptance_criteria>
    - `frontend/src/index.css` contains `.card {`
    - `frontend/src/index.css` contains `.form-input {`
    - `frontend/src/index.css` contains `.status-dot {`
    - `frontend/src/index.css` contains `.table {`
    - `frontend/src/index.css` contains `.modal {`
    - `frontend/src/index.css` contains `.toast {`
    - `frontend/src/index.css` contains `.sidebar {`
    - `frontend/src/index.css` contains `@media (max-width: 768px)`
    - `wc -l frontend/src/index.css` shows file grew by ~200+ lines
  </acceptance_criteria>
  
  <done>Complete component class library added to index.css covering cards, forms, status indicators, tables, modals, toasts, layout, and utilities</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS injection | User-provided content rendered in styled components |
| XSS via innerHTML | Dynamic content in JSX could bypass CSS sanitization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ui-01 | T (Tampering) | CSS variables | accept | CSS variables are not security-sensitive; visual styling only |
| T-ui-02 | I (Info Disclosure) | Error messages | mitigate | Toast notifications hide technical details per D-06 |
</threat_model>

<verification>
1. `test ! -f frontend/src/styles/main.css` — duplicate deleted
2. `grep -c '#333333' frontend/src/index.css` returns 0 — no hardcoded border colors
3. `grep -c "\.card {" frontend/src/index.css` returns 1 — card class exists
4. `grep -c "@media" frontend/src/index.css` returns 1 — responsive breakpoint defined
</verification>

<success_criteria>
- Single CSS file with all design tokens
- No duplicate CSS files
- All hardcoded colors replaced with variables
- Component classes ready for page extraction
</success_criteria>

<output>
After completion, create `.planning/phases/ui-redesign/01-01-SUMMARY.md`
</output>
